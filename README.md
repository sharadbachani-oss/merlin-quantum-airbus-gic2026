# Airbus — current package

Read **AIRBUS_PROPOSAL_v7.md** (source) or **report.pdf** (the Phase-1 concept proposal as submitted, v7.5). These supersede SUBMISSION_NATIVE_v6.md and the historical narratives in legacy_reports/. Run `python verify.py` (numpy only) to replay the headline numbers from archived counts.

Status: P4_HARDWARE_RETRIEVED_UNIFORM_SPECTRUM_AND_FLUID_BRIDGE_OPEN

Next: Uniform k=0..16 series plus no-exchange and decoupled controls; separate fluid-model mapping test.

Evidence and code are retained. Historical flight scripts are not automatically validated for a new experiment; use NATIVE_EXPERIMENT.json as the current contract. Shared tools are in the parent directory.
